# Copy to ~/.config/macos-config-sync/config.
# This deliberately small example REPLACES the corresponding default arrays.
# All managed paths are relative to HOME; scopes must not overlap.
MANAGED_DIRECTORIES=('scripts' 'docs')
MANAGED_FILES=('.zshrc' '.gitconfig')
MACHINE_DIRECTORIES=()
MACHINE_FILES=('Brewfile' 'installed-apps.txt' 'Moom.plist')
EXPLICIT_DIRECTORIES=()
EXTRA_EXCLUDE_PATTERNS=('*.local')
AUTO_ADD=0
