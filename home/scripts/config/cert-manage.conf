# Script: cert-manage.sh
# Purpose: Shared defaults for RSA 4096 certificate management.
# Version: 1.0.0
# Values are literal. No shell expansion or commands are allowed.
# The script expands a leading ~/ in path settings.
# Relative paths are resolved against the script directory.

ACME_HOME='~/.acme.sh'
OUTPUT_ROOT='~/certificates'
CERT_CONFIG_DIR='config/certificates'
DNS_SLEEP=180
